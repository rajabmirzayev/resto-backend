databaseChangeLog:
  - changeSet:
      id: 008-add-password-to-users
      author: rajabmirzayev
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: password
                  type: VARCHAR(255)
                  remarks: BCrypt hashed password for local auth
